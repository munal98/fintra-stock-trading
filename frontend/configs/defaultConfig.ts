const defaultConfig = {
    baseUrl: 'http://localhost:8080/api/v1',
    name: 'Fintra Stock Trade',
    version: '1.0.0',
    tokenKey: 'auth_token',
    mode: 'development',
};

export default defaultConfig;
